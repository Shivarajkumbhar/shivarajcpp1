#include<iostream> 
using namespace std; 
int main()
{ 
  // variable declaration 
  int a = 20, b = 30, c;

// Largest among a and b 
  c = (a > b) ? a : b;

// Print the largest number
cout << "Largest number between a and b is "<< c << endl; return 0;
}
